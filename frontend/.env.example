# Public values only. VITE_* variables are embedded in the browser bundle.
VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
# The pretrained ONNX model is bundled with the frontend for reliable demo inference.
VITE_AI_MODEL_URL=/ai-model
